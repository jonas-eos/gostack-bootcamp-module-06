import React from 'react';
import { View, Text } from 'react-native';

/**
 * @const Main
 * @type {component}
 * @default
 */
const Main = () => <View />;

Main.navigationOptions = {
  title: 'Users',
};

export default Main;
