import React from 'react';
import { Container } from './styles';

/**
 * @const Main
 * @type {component}
 * @default
 */
const Main = () => <Container />;

Main.navigationOptions = {
  title: 'Users',
};

export default Main;
